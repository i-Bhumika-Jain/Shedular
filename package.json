{
    "name": "schedular",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "backend:install": "cd backend && uv sync",
        "backend:migrate": "cd backend && uv run python -m app.db.migrate",
        "backend:dev": "cd backend && uv run python -m uvicorn app.main:app --reload --port 8000",
        "frontend:install": "npm install --prefix frontend-next",
        "frontend:dev": "npm run dev --prefix frontend-next",
        "frontend:build": "npm run build --prefix frontend-next"
    }
}
